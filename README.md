# graduate_cource
git push
git commit -am "标签"
git status
git add .
git pull

$ git rm -cached filename

$ git clone github_link.git

$ git log

$ git status

$ git add .

$ git commit -am "message"

$ git push

$ git pull
